spring:
  application:
    name: bank-api
  datasource:
    url: jdbc:mysql://localhost:3306/bankapi
    username: root
    password: Kittu@2003
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  jwt:
    secret: your-very-long-secret-key-at-least-32-chars
    expiration: 86400000

server:
  port: 8080